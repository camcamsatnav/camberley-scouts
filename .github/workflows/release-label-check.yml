name: Check release label

on:
  pull_request:
    types:
      - opened
      - reopened
      - labeled
      - unlabeled
      - synchronize
      - ready_for_review
    branches:
      - main

jobs:
  check-label:
    runs-on: ubuntu-latest

    steps:
      - name: Check for exactly one release label
        uses: actions/github-script@v7
        with:
          script: |
            const allowed = ['release:patch', 'release:minor', 'release:major'];
            const labels = context.payload.pull_request.labels.map((label) => label.name);
            const releaseLabels = labels.filter((label) => allowed.includes(label));

            core.info(`PR labels: ${labels.join(', ') || '(none)'}`);

            if (releaseLabels.length !== 1) {
              core.setFailed(
                `Expected exactly one release label: ${allowed.join(', ')}. Found: ${releaseLabels.join(', ') || '(none)'}.`
              );
            }
